# MVP implementation status

## Technical completion

Status as of 2026-09-05: the technical MVP is complete at commit `1cc5dc6`. All planned implementation increments and final product-completion verification passed. Remaining production ownership, infrastructure, approved content, and legal values are launch-readiness inputs rather than technical MVP blockers.

## Verified increments

### Foundation

- Exact pnpm workspace and dependency pins are tracked.
- Neutral Go module, typed configuration, normalized public base paths, structured logging, health endpoints, graceful shutdown, and non-root container runtime are implemented.
- Root Makefile, environment example, Docker ignore rules, Compose topology, pinned service images, and Nginx SPA routing are implemented.
- Frontend production builds work for `/` and `/ause-discovery/`.
- The production-like Compose stack builds and starts with healthy PostgreSQL, Meilisearch, and API services.
- Default-base-path liveness, readiness, redirect, SPA delivery, and nested-route fallback are verified through Nginx.
- Frontend TypeScript checking, JavaScript configuration linting, seven base-path tests, and the production build pass.

### Data and contracts

- OpenAPI 3.1 defines the stable public, authentication, administrator, Artifact, import, search, audit, and health endpoints.
- Generated Go transport types and Chi server interfaces are isolated from generated sqlc models; generated TypeScript client and wire types compile.
- sqlc queries, generated pgx code, transaction handling, UUIDv7 generation, and one forward Goose migration implement the accepted persistence boundaries.
- Fresh PostgreSQL migration, repeat migration, representative constraints, open-ended academic versions, single-active-rebuild enforcement, and transaction rollback pass integration tests.
- Version-controlled academic catalog and taxonomy validation plus non-destructive synchronization are implemented through `ausectl` and pass repeat-sync integration tests.
- `ausectl migrations status`, `catalog validate`, and `catalog sync` pass in the pinned backend image.
- Generated sqlc, Go OpenAPI, and TypeScript OpenAPI output is deterministic across repeated generation.
- A fresh isolated Compose project builds, migrates, starts healthy, synchronizes catalogs twice, and serves health and nested SPA routes under `/ause-discovery/`.

### Identity and core records

- Argon2id local credentials, opaque hashed sessions, independent CSRF tokens, idle and absolute expiry, session rotation, revocation, and database-backed login throttling are implemented.
- Operator CLI commands create, reset, and disable administrator accounts without accepting passwords through flags or environment variables.
- Append-only audit events cover identity, Person, and Project mutations, including bootstrap operations without an existing actor.
- Person create, update, get, and list behavior enforces Student ID and optimistic revision rules.
- Project aggregate create, replace, publish, delete, and restore behavior maintains ordered aliases, participation, taxonomy, revision, audit, and pending search state transactionally.
- Public Project and Person projections remain PostgreSQL-backed and enforce published visibility.
- HTTP transport enforces strict JSON, Problem Details, request IDs, security headers, authenticated mutation CSRF, same-origin checks, and trusted proxy boundaries.
- Administrator Project responses include complete academic, participation, taxonomy, and Artifact state for safe form round trips.
- The frontend provides search-first public routes, public Project and Person details, protected administration, Person maintenance, complete Project aggregate controls, revision-conflict preservation, and Project-specific delete confirmation.
- Backend tests pass against isolated temporary PostgreSQL databases. Frontend static checks, 20 component tests, root and subpath production builds, and the production-like Compose image build pass.
- Authenticated HTTP smoke verification covers administrator creation and login, Person creation, Project draft creation, publication, public Project and Person reads, deletion visibility, restoration, and logout.

### Artifacts

- Opaque versioned filesystem keys, path containment, symlink rejection, streamed SHA-256 hashing, atomic finalization, per-file limits, and compensating cleanup protect Artifact bytes.
- Artifact type and extension allowlists reject executable, HTML, SVG, script, disk-image, and macro-enabled Office uploads. Bounded server-side content detection rejects unsafe or mismatched file content.
- Upload quota checks serialize through the Project row and maintain Project revisions, pending search state, and append-only audit records in the metadata transaction.
- Metadata update, reversible deletion, quota-checked restoration, and immutable replacement preserve optimistic concurrency and existing bytes.
- Public serving requires an active Artifact on a published Project. PDF inline view, safe UTF-8 download filenames, byte ranges, missing-content handling, and no-sniff responses are implemented.
- Administrator Project editing includes accessible Artifact upload, metadata update, replacement, delete, and restore controls. Public Project pages expose PDF view and allowed Artifact download actions.
- Backend unit and isolated PostgreSQL integration tests cover storage safety, size and type validation, lifecycle revisions, quota, search state, audit records, immutable replacement, cleanup, and public visibility.
- Frontend static checks and 22 component tests pass. Authenticated HTTP smoke verification covers upload, partial PDF view with a `206` response, metadata update, replacement, public deletion visibility, restoration, and full administrator aggregate retrieval.

### Search

- PostgreSQL remains canonical while Meilisearch stores derived, rebuildable Project documents with a stable logical index and versioned physical rebuild indexes.
- Published Project projections include aliases, academic references, ordered People roles, taxonomy, active Artifact availability, revisions, and sort fields while excluding private metadata and storage identities.
- Query validation, exact seven-digit identifier precedence, deterministic cursor binding, OR-within-facet and AND-across-facet filtering, sorting, facet distribution, and bounded highlights are implemented.
- The reconciliation worker uses leases, bounded retries, changed-revision protection, expired-lease recovery, and removal of no-longer-public Projects.
- Full rebuilds configure a replacement index, populate stable pages, reconcile final published state under serialization, verify document counts, swap indexes, preserve the prior physical index, and record operation status and audit events.
- Public search, authenticated status, Project reindex, and rebuild HTTP endpoints are implemented. The operator CLI supports Project reindex and full rebuild commands. Administration includes search availability, queue counts, Project reindex, rebuild progress, and failure display.
- Focused backend tests, the full backend suite, all 23 frontend tests, TypeScript checking, and the production frontend build pass.
- Local PostgreSQL and Meilisearch smoke verification covers queued reconciliation to `synced`, filtered and alias search, exact Student ID search, complete rebuild, verified swap, and authenticated status with no pending or failed rows.

### Imports

- Strict CSV and XLSX adapters enforce exact headers, supported workbook structure, formula and macro rejection, bounded files, Student ID text preservation, and canonical Project import drafts.
- Persistent PostgreSQL previews retain row validity, warnings, duplicate candidates, selections, acknowledgements, and duplicate resolutions across process restarts.
- Commit revalidates selected rows against current catalogs, creates People and Projects in one transaction, publishes complete Projects, records search reconciliation and audit state, and returns an immutable result on repeated requests.
- Expired batches scrub temporary source files and persisted draft payloads while retaining bounded operational metadata.
- HTTP endpoints provide upload, paginated preview, row-decision updates, atomic commit, stable results, cursor validation, Problem Details, authentication, CSRF protection, and the `import.execute` session capability.
- Administration provides template downloads, client-side file checks, persistent review pagination, warning acknowledgement, duplicate resolution, selection saving, guarded commit, and stable result display.
- Bundled CSV and XLSX templates use shipped catalog keys and pass the production import adapters.
- Focused adapter and PostgreSQL integration tests cover valid and rejected files, restart persistence, correction state, all-or-nothing rollback, repeated commit, search state, and expiry cleanup. Focused HTTP tests, the import interaction test, and TypeScript checking pass.

### Audit administration

- `GET /admin/audit-events` requires an administrator session, filters by exact action and actor UUID, and pages with a stable keyset cursor ordered by `created_at DESC, id DESC`; malformed cursors return `400 validation_error` and missing sessions return `401`.
- The audit response exposes nullable `actor_id` for bootstrap and failed-login events, always-present `resource_type`, nullable `resource_id`, and a required metadata object, with `event_type`, `target_type`, and `target_id` mapped at the HTTP boundary.
- Session and login responses include `audit.read` in the single administrator permission list.
- `/admin/audit` renders the real audit interface with exact filters, actor UUID normalization, explicit Apply, Clear, and failure Retry controls, system-actor labeling, text-only metadata in a compact definition list, cursor Next and Previous navigation, and loading, empty, invalid-filter, and request-error states.
- Audit storage remains append-only: the SQL surface exposes no update or delete path for `audit_events`.
- Focused verification: audit service unit tests, one PostgreSQL integration test for ordering, filters, nullable values, limit behavior, and two-page cursor pagination without duplicates; an HTTP boundary integration test covering 401, 400 cursor and parameter validation, successful null-actor and metadata mapping, and `audit.read`; the frontend audit interaction test; `tsc -b`; and an authenticated Compose smoke path verifying the unfiltered listing, exact action filter, cursor paging, bad-cursor `400`, missing-session `401`, and invalid actor `400` through a disposable administrator and disposable database.

### CI and operations

- `validate.yml` runs for pull requests and for default-branch pushes routed through the event context (`github.ref_name == github.event.repository.default_branch` on each job, so no branch name is hardcoded and tag pushes are excluded), covering frontend source checks (Node 24.20.0, pnpm 11.25.0, action-pin validation, lint, component tests, default and root base-path builds), backend checks (Go 1.27.0 container, `go mod verify`, tracked-file `gofmt`, `go vet`, full tests with a digest-pinned PostgreSQL 18.6 service and a non-optional `AUSE_TEST_DATABASE_URL`), generated-code drift for sqlc and both OpenAPI outputs, API and web image builds with root web variant using GitHub cache, and vulnerability reporting: gating `govulncheck` v1.7.0 plus advisory `pnpm audit` and Trivy image scans with explicit `continue-on-error` rationale.
- `release-images.yml` triggers only on `v*` tags or manual dispatch defaulting to no push, grants `packages: write` only to the publication job, derives GHCR names from `github.repository`, publishes version and commit-SHA tags without `latest`, applies OCI `source`, `revision`, `version`, and image-specific `title` labels from runtime context, emits digests into the workflow summary, and contains no deployment step.
- Every external action is pinned to a full commit SHA with a release comment; `scripts/check-action-pins.sh` enforces this for tracked workflows and a fixture test proves acceptance and rejection behavior.
- `compose.yaml` consumes `AUSE_API_IMAGE` and `AUSE_WEB_IMAGE` overrides, accepts complete digest references while keeping local defaults and builds, parameterizes the web host port, and adds bounded log rotation defaults; `.env.example` documents production-required values and image digest consumption with `docker compose pull` and `up -d --no-build`.
- Local CI parity targets (`check-source`, `check-integration`, `generate-check`, `check-images`, `check-compose`, `vuln-report`, `check-action-pins`, `check-action-pins-test`) cover the locally reproducible CI boundaries; `vuln-report` covers govulncheck and the pnpm audit while the Trivy image scans run in CI only. `generate-openapi-typescript` invokes the pure-JavaScript generator shim in the pinned Node container instead of containerized pnpm.
- `docs/operations/runbook.md` covers scope invariants, host prerequisites, configuration, ordered bootstrap, routine operations, health, backup targets, restore ordering, upgrade and rollback constraints, recovery scenarios, shutdown, and the manual deployment boundary using repository service names and `ausectl` commands.
- Focused verification passed: `git diff --check`; actionlint v1.7.12; repository and fixture action-pin checks; Compose configuration rendering with local defaults and with production-like dummy values including digest-shaped API and web references; frontend lint, component tests, and both production builds; Go module verification, tracked-file formatting, vet, unit tests, and PostgreSQL-backed integration tests; generation drift check; API and web image builds including the root base-path variant; govulncheck (zero reachable findings), pnpm audit, and Trivy scans of both built images.
- Final dependency verification is clean: `golang.org/x/crypto` is updated to 0.55.0, the workspace pins `js-yaml` 4.3.1 through a workspace override, govulncheck reports no reachable vulnerabilities, pnpm audit reports no known vulnerabilities, and the final API and web runtime images report zero high or critical Trivy findings.

### Product coherence

- One shared application frame renders the site header, primary and footer navigation, a single focusable `<main id="main-content">` landmark, and a `Skip to main content` link for public, login, administrator, and Not Found routes; route changes move focus to the main region once per path change without stealing focus during refetches.
- Every named route sets a specific document title, administrator secondary navigation stays inside the main region, the unexpected-error boundary renders a minimal recovery page, and About, Privacy, Accessibility, Terms, and a new Contact route present factual content with visible pending-approval notices where institutional content is required.
- A generated-client error interceptor installed exactly once clears cached administrator session state on any API `401`, so `AdminGuard` returns to `/admin/login` with the full intended path in `next`; failed logins keep the generic credential message and public `401` responses cause no redirect loop.
- Search uses a draft query field applied on submit, distinguishes first load from background refresh, reports catalog loading or failure states, renames paging to `Next page`/`Previous page` with local cursor history that resets on filter changes, disables paging during requests, and renders API abstract highlights as text-only excerpts.
- Public Project and Person pages translate participation roles, render Reference Code and Student ID only when present, distinguish Not Found from request failure through the problem code, and open PDF View links in a new browsing context with `noopener noreferrer`; the same behavior applies to administrator Artifact View links.
- Administrator Project and People lists gained query and status filters with explicit Apply and Clear, shared cursor-history paging, and loading, empty, and request-failure states without false empties; the backend now implements the declared list contract: Projects honor `q` (trimmed, case-insensitive title or reference code), `status`, `cursor`, and `limit`, People honor `q`, `cursor`, and `limit`, both order deterministically with a stable tie-break, fetch limit plus one, emit `next_cursor` only when another page exists, and reject malformed cursors with `400 validation_error` through shared opaque `pagecursor` payloads bounded at 4096 characters, a size derived from the worst contract-legal sort key as actually serialized, so maximum-length Unicode and JSON-escaped Person names page correctly; Person creation and editing validate locally, disable while pending or without CSRF, show saved status, and preserve entered values across a revision conflict while an explicit reload awaits the actual server response through a separate cache entry, adopts its fields and revision only on success, disables submission while reloading, and preserves entered values with a retry notice when the reload itself fails; each reload owns the shared loading flag by request token so an older request cannot enable submission while a newer reload is still pending; navigation between Person routes adopts the new record's fields, revision, and clean feedback state, and an outstanding reload for a previous Person updates only the cache; the Project form blocks behind Catalog and People loading with an explicit dependency retry, reuses the revision returned by a successful edit for the next save, publish, and lifecycle operation, and lifecycle revision conflicts offer explicit reload without discarding unsaved form input, with the confirmation dialog closing on failure and restoring focus to its trigger so recovery controls are reachable; the reload uses a separately handled fetch that cannot push the observed Project query into its error state, so a failed reload preserves the form, entered values, and reachable retry feedback, while a successful reload adopts the current record and clears the conflict notice and stale mutation error; Artifact operations and sign out gained pending and failure feedback without discarding sessions or input.
- The delete confirmation traps keyboard focus among enabled controls only, including the single-enabled-control pending case, focuses the confirmation input initially, closes on Escape through cancel, restores the triggering control, blocks background pointer activation, disables actions while pending, and ignores Escape and backdrop dismissal while the deletion request is pending; its controls are explicitly `type="button"` so they can never submit an enclosing form.
- Vite and React starter assets were removed after tracked-reference checks, and the favicon is a repository-owned neutral mark; the focus ring moved to a semantic token, disabled controls gained non-color state, and long identifiers and titles wrap without page-level horizontal overflow.
- Nginx serves all static and proxied responses with `nosniff`, `same-origin` referrer, permissions policy, and a restrictive CSP through an included snippet repeated in caching locations so `add_header` inheritance cannot drop it; the entry document stays `no-cache`, assets stay immutable, redirects are port-agnostic, and no HSTS is emitted.
- The web container runs the pinned Nginx image entirely as the unprivileged `nginx` account on internal port `8080` with narrowly granted runtime directory ownership; Compose maps the published web port to the new internal port.
- Focused verification passed: `git diff --check`; `tsc -b`; ESLint; all component test files (21 files, 59 tests after corrections) including the new shell, session-expiry, search paging and history synchronization, cursor hook, Project and People lists, Project form dependency and lifecycle boundaries, Person form conflict reload, public presentation, and delete-confirmation boundaries; both production base-path builds; both web images built without publication; Compose config rendering with the new internal port; and a disposable container smoke confirming the `nginx` process user, `nginx -t`, the 8080 listener, entry and nested SPA delivery, immutable asset caching, security headers on home, nested, and asset responses with no HSTS, for both default and root base-path images.
- Correction verification passed: backend package tests for `pagecursor`, People, Projects, and the HTTP server covering q/status filtering, two-page cursor pagination without duplicates, deterministic ties, final-page behavior, malformed-cursor `400` responses, and maximum-length Unicode names producing usable in-bounds cursors; `gofmt` clean; generation drift clean after the cursor-bound contract adjustment; and an authenticated HTTP smoke against a disposable administrator and database proving Projects and People `next_cursor` round trips at small limits, uppercase `q` filtering, and malformed-cursor rejection on both lists. A second correction round verified the awaited Person reload (deferred response with renamed fields and a newer revision, next save using both), reload-failure input preservation, dialog-closed lifecycle recovery with focus restoration, enabled-only focus trapping in both multi- and single-control cases, and the Unicode cursor bound. A third correction round verified record-aware Person navigation (correct fields, revision, and save target after route change, with an outstanding previous-record reload leaving the new form untouched), the 4096-character cursor bound covering six-byte JSON escapes with escaped and multibyte round-trip coverage at unit and integration levels, and stale lifecycle error clearing after successful recovery. A fourth correction round, using deferred-promise tests, verified that an older Person reload cannot clear a newer reload's loading state or fields after navigation, and that a failed Project lifecycle reload preserves the form with retry feedback before a successful retry adopts the current revision for the next save.

### Final product completion

- A fresh isolated Compose stack built, migrated, synchronized version-controlled catalogs, started healthy, and preserved PostgreSQL, Meilisearch, import-result, session, and Artifact state across a complete stack stop and restart without volume removal.
- Authenticated live acceptance created People and a complete Project, uploaded and range-served a PDF Artifact, published the Project, found it through public search, rendered public Project and Person pages, and verified safe view and download behavior.
- CSV and XLSX imports produced valid persistent previews; an XLSX selection committed atomically, returned an immutable result on repetition, and became publicly searchable.
- Search-unavailable behavior returned a controlled `503` while PostgreSQL detail remained available. A full CLI rebuild restored the derived index. Project delete and restore transitions synchronized public detail, Artifact visibility, and search results.
- Root and `/ause-discovery/` deployments served entry, nested SPA, login, health, API, and Artifact routes. Playwright acceptance passed three tests against each deployment at desktop and 360-pixel widths with no axe violations.
- Final stabilization corrected Compose digest and configuration forwarding, implemented the previously missing Catalog and versioned health handlers, rejected unsupported schema versions at startup, expanded readiness to schema and writable-storage checks, and made the container health check use the live HTTP readiness route.
- The final API runtime uses a minimal pinned Alpine image with current OpenSSL packages, the refreshed pinned Nginx image is clean, and Trivy reports zero high or critical findings for both final images and both Go binaries. Govulncheck reports no reachable vulnerabilities and pnpm audit reports no known vulnerabilities.
- Full PostgreSQL-backed Go tests, frontend lint, type checking, 59 component tests, root and subpath builds, deterministic generation, Compose rendering, action-pin checks, dependency verification, image builds, live acceptance, restart recovery, and browser acceptance passed. No external deployment, publication, registry push, or production mutation occurred.

## Post-MVP maintenance

- The `migrate` service no longer declares its own build block because it shares `AUSE_API_IMAGE` with `api`; parallel export of the same tag made `docker compose build`, `make seed`, and `make compose-up` fail with `image ... already exists`. Local builds and the documented runbook rehearsal path (`docker compose build`) now build only `api` and `web`, verified against a disposable local project.

### Current disposable-data rebuild policy

Recorded 2026-09-12: no current PostgreSQL, Meilisearch, local Artifact, or B2 data requires preservation. The reviewed metadata CSV and the versioned Project Content manifest plus its referenced local files are the development sources of truth. A fully fresh rehearsal may stop the application, remove the intended Compose project's volumes, empty every object version from the B2 bucket, bootstrap and synchronize catalogs, import Project metadata from the reviewed CSV, then import logos and Project Files from the Project Content bundle. PostgreSQL owns the mapping from stored objects to Projects, so preserving B2 while wiping PostgreSQL leaves unreferenced objects and does not provide an idempotent rebuild. This disposable-data policy remains active until the maintainer explicitly declares a non-disposable production dataset or manual administrative changes that must survive rebuilds.

## Active implementation work

- No technical MVP implementation work remains. Launch readiness begins when the external inputs listed below are available.

## Known implementation constraints

- The host Node.js version is below the exact repository baseline; the pinned container toolchain provides the reproducible path.
- Go is absent on the host; Go verification uses the pinned container toolchain.
- `typescript-eslint` 8.69.0 rejects TypeScript 7.0.2. The current lint gate runs ESLint on compatible JavaScript configuration and uses `tsc -b` for TypeScript static checking while preserving the accepted exact pins.
- `@hey-api/openapi-ts` 0.99.0 rejects TypeScript 7.0.2. Reproducible client generation uses the isolated `api/generator` workspace with TypeScript 5.9.3 while application compilation remains on TypeScript 7.0.2.

## Launch-only inputs

GitHub ownership, registry ownership, production URL, VM details, branding, approved legal content, privacy approval, TLS ownership, and final institutional catalog content remain launch-readiness inputs rather than technical MVP blockers.

## Post-MVP improvement backlog

Recorded 2026-09-05 after the SP metadata extraction work in `tools/sp-import/`. The product specification defers exact vocabularies (section 59) and the roadmap (section 41) remain the governing documents; this list tracks concrete accepted follow-ups so any session can pick them up.

Applied to version-controlled catalogs, pending the next `ausectl catalog sync` (`make seed`) to reach PostgreSQL:

- `config/catalogs/academic.yaml` now covers pre-2020 years (`valid_from_year: 1990`) and adds the `information_technology` program; the `senior_project` course version links both program versions. The SP corpus (2016-2025, IT and CS departments) imports against both programs.
- `config/taxonomy/values.yaml` gained nine keys: topic `recommender_system`; technologies `nextjs`, `nodejs`, `mongodb`, `laravel`, `wordpress`, `kotlin`, `mysql`, `unreal_engine`. Extension policy: only abstract-verifiable implementation stacks, no overlap with broader facets (Raspberry Pi and wearables stay under `embedded_hardware`, sentiment analysis under `nlp`, and so on). The extractor's unmapped-vocabulary report lists deliberately rejected candidates.

Status ledger:

1. **Operational, recurring.** Run `make seed` or `ausectl catalog sync` on each fresh stack bring-up. Catalog synchronization is implemented, additive, and non-destructive.
2. **Implemented 2026-09-12.** The catalog now defines `senior_project_1`, `senior_project_2`, and the unspecified `senior_project`; the completed corpus pass assigns every imported Project through explicit statements, course-code evidence, or the documented unspecified fallback. Final institutional vocabulary review remains a launch input.
3. **Superseded by the completed corpus pass.** Final reports and the three slide-only Projects were processed. The remaining five drops lack required academic period evidence rather than merely waiting for proposal replacement.
4. **Implemented.** The full corpus extraction and agent review are complete; current counts and exclusions are recorded below under corpus extraction status.
5. **Resolved or accepted.** The later corpus pass confirmed 1934's title and 2238's full student list; 26010 retains the selected approval title. Abstract-based classification remains the deliberate evidence boundary.
6. **Open.** Production proxy adjustments from the deployment review: bind the published web port to loopback only, and preserve `X-Forwarded-Proto` through the container Nginx so HTTPS same-origin validation holds.
7. **Implemented.** Public Project details now render nonempty classification groups under localized dimension names. Major and Topic remain absent from public interfaces by product decision.
8. **Implemented and accepted.** Artifact storage became a pluggable interface with a per-Artifact backend marker. Decided in AD-011 (2026-09-10, supersedes AD-008). Implemented 2026-09-10 in commit `f021ba7` per the brief `.memory/docs/increments/10-pluggable-artifact-storage.md`. VM-local primary storage and S3-as-a-service were rejected by the institution's other admin; Google Drive was evaluated and rejected (consumer-account lock exposure, OAuth refresh-token fragility, per-user API rate limits, no institutional second copy). Provider selected: Backblaze B2 (no card required, first 10 GB always free, free egress up to 3x average monthly storage sufficient at current scale, R2's unmetered-egress advantage unusable at this scale and requires a payment method). Implementation: `Backend` interface (`Put`, `PutAt`, ranged `Open` returning `io.ReadSeekCloser`, `Exists`, `RemoveNew`) with `LocalStorage` and `B2Storage` (pinned aws-sdk-go-v2: core v1.47.0, credentials v1.20.4, service/s3 v1.113.0) behind `StorageSet` routing by the `artifacts.storage_backend` marker (migration 00002, CHECK-constrained to local/b2); upstream B2 failures map to `503 artifact_storage_unavailable`; `ausectl artifacts migrate [--apply]` copies local bytes to the default provider under identical keys with digest verification and guarded idempotent stamping. Live local Compose verification covered B2 upload, public and ranged reads, restart persistence, migration, mixed-provider serving, and controlled outage recovery. Release publication and VM rollout are operational tasks rather than missing implementation.
9. **Partially implemented.** Storage readiness now checks the configured provider per item 14. Two required correctness fixes remain open: advance the B2 ranged reader's logical offset after each successful read, and replace boolean existence checks with a contract that distinguishes missing content from provider unavailability. Changing the configured default still does not provide failover for existing provider-stamped rows. Automatic second copies, reverse migration, and fallback tooling remain deferred. B2 upload validation stages each file in the container operating system temporary directory rather than `AUSE_IMPORT_TEMP_ROOT`; that accepted disk requirement is documented.
10. **Open.** Storage terminology refactor is due. `Backend` is reserved for the AUSE Backend API, the application service connecting the frontend, PostgreSQL, search, and supporting services. It must not refer generically to a B2 bucket or local filesystem. Use `Artifact store` for the byte-storage concept, `storage provider` for a configured implementation such as `local` or `b2`, and `storage adapter` for the Go implementation. Required rename scope includes the storage interface and implementations, `StorageSet`, `storage_backend` database marker, configuration names and comments, CLI output, internal operator notes, tests, and user-facing wording. Preferred direction: `ArtifactStore` or `StorageProvider` interface, `LocalArtifactStore`, `B2ArtifactStore`, and `storage_provider`. This is not optional polish. The overloaded name obscures architecture and must be removed in a coherent follow-up refactor.
11. **Implemented as Increment 14, 2026-09-12** (`.memory/docs/increments/14-docx-report-support.md`). The shared `report` extension allowlist now accepts `pdf` and `docx` (`backend/internal/artifacts/validation.go`), inheriting the existing DOCX detection branch: the detected content must be the ZIP-compatible Office container (`application/zip` plus the PK signature), and executable, HTML, SVG, and script content stays rejected regardless of filename. `.doc`, `.docm`, `.zip`, and extensionless files remain rejected as reports; `.DOCX` follows the existing case-insensitive normalization. Administrator upload, replacement, and Project Content import all inherit the behavior from that one shared path with no compatibility branches. PDF remains the only inline-viewable report format: `view_url` requires PDF MIME plus extension, the public View route rejects non-PDF Artifacts with 404, and DOCX reports expose Download only with attachment disposition and the sanitized original filename, exact bytes preserved. The frontend needs no DOCX branch because it renders actions from the response. Verification: unit tests for extension acceptance (including uppercase normalization) and rejections; inspection tests through `InspectUpload` with a standard-library-generated DOCX fixture covering acceptance, mismatched content, and unsafe content named `.docx`; an `artifactResponse` test proving Download-only URL mapping; a PostgreSQL-backed HTTP test proving View 404 and Download 200 with attachment disposition and byte-exact content through the real handler; and a Project Content test planning and applying a `report` docx entry that stores with extension `docx`. Full `go test` for `artifacts`, `platform/httpserver`, and `projectcontentimport` passed against the disposable PostgreSQL database. Preferred practice unchanged: convert to PDF where practical for the canonical viewable report and attach the submitted DOCX as a second report when original bytes matter; multiple active reports are already supported.
12. **Implemented and accepted.** Local B2 bulk demonstration seeding was accepted by maintainer testing on 2026-09-11. A fresh schema-version-2 `ause-local-test` deployment imported 209 published Projects, dry-run planned 836 uploads, apply uploaded all 836 without error, public behavior passed visual and functional testing, and a repeated apply skipped all 836. Operator feedback: the apply phase appears hung because the current importer uploads sequentially and prints only the final summary. Implemented 2026-09-11 per `.memory/docs/increments/11-project-file-import-concurrency.md`: `artifactimport` now groups the validated plan by Project (first-appearance dispatch order, file order preserved), dispatches groups through a fixed worker pool (`normalizeWorkers`, default 4, clamped to 8) with one goroutine per Project's sequential file loop keeping the final `shouldSkip` check and fresh revision read before every upload, and folds results through a single coordinator goroutine that assigns completion positions and invokes the Project-completion callback (never overlapping; `OnApplyStart` instead runs synchronously on the caller after planning and immediately before dispatch; the service owns no printing). The coordinator controls assignment itself, priming the pool and issuing each replacement assignment after a completed outcome, so no new Project is assigned after a fatal outcome (context cancellation or an error matching `artifacts.ErrStorageUnavailable`) reaches the coordinator; ordinary isolated failures stop only their Project and the command exits nonzero with a bounded summary carrying the first safe error. `--workers` on `artifacts seed-demo` and `artifacts import-manifest` accepts 1 through 8 only (set-once flag value, rejected values fail before any database or storage work), apply prints one start line plus exactly one sanitized single-line progress event per completed Project carrying the Project ID beside the title so duplicate titles stay distinguishable, with every entry reporting Artifact type and original filename, skips their reason, failures the controlled error, control characters collapsed, and lengths bounded. Dry-run and the final aggregate summary keep their existing labels with failed counts added when nonzero, and `Result` gained `FailedProjects`/`FailedFiles`. Planned skips are counted once by planning and reported on progress lines without recounting. Verification 2026-09-11: unit tests for worker normalization, group ordering, bounded concurrency with serialized once-per-Project callbacks (completion positions 1..N), fatal-outcome dispatch stop, and context cancellation without goroutine leakage; parser tests for the workers flag including repeated-flag rejection and migrate rejection; formatting tests for progress-line grouping, sanitization, and bounding; PostgreSQL integration tests covering the two-Project multi-file apply and repeatable rerun with progress events under workers 4 and 2, and a deterministic targeted ordinary failure that leaves committed uploads intact, exits nonzero, and reruns to completion; race detection passed for both focused packages inside the pinned container with gcc installed ephemerally; whole-backend `go vet` and `gofmt` clean; `git diff --check` clean. Correction round from independent review, applied the same day: the coordinator now owns replacement assignment (no feeder goroutine, so no new Project can be assigned after a fatal outcome reaches it) proven by a deterministic channel-gated immediate-failure test without timing sleeps; every progress line carries the Project ID and every entry the Artifact type and original filename; and the recovery integration test now registers a healthy b2-named adapter over the same root, asserts the previous run's rows are stamped b2 and the rerun's upload local, and opens all three Artifacts through the recovery set with exact content. Final correction round: the coordinator re-checks run-context cancellation before priming and before every replacement assignment, so a parent context canceled by a worker returning a non-fatal outcome (or before dispatch) cannot start further Projects; cancellation stops assignment, closes the jobs channel once, keeps collecting already-running outcomes, and `Run` returns a nonzero cancellation error when no Project failed, with deterministic unit tests proving exactly one Project runs after an in-worker parent cancellation and zero run on a pre-canceled context.
13. **Implemented and accepted.** Project Logo integration implemented 2026-09-12 per `.memory/docs/increments/12-project-logo-integration.md` (split author-review workflow: the brief was reviewed independently; this session implemented it). A Project Logo is now a separate Project-owned concept: migration 00003 adds `project_logos` (storage_key unique, storage provider marker, PNG-only CHECKed metadata, active/deleted status, one active row per Project through a partial unique index, revision counter), schema version 3. `projectlogos.Service` validates PNG content (exact `image/png` detection, 2 MiB, 1600 px), stores bytes through the shared storage set, soft-deletes the previous row, derives each new revision from every historical row for the Project so `logo_url?v=` strictly increases across upload, replacement, removal, and re-upload and no public version is ever reused, advances the Project revision, queues search synchronization, and appends `project_logo.uploaded/replaced/removed` audit events. `GET /projects/{id}/logo?v=N` serves only the matching active revision of a published Project with `Cache-Control: public, max-age=31536000, immutable` and inline PNG disposition; the expected revision is part of the metadata lookup, so a missing or stale `v` (controlled 400 for the required parameter, 404 for stale or absent values) never opens the storage provider; absent/draft/deleted Projects, removed logos, and logo-less Projects return 404, and provider failure maps to 503 `project_logo_storage_unavailable`. A separate authenticated `GET /admin/projects/{id}/logo` serves the current active logo to administrators with `Cache-Control: no-store`, including on draft Projects, and `AdminProject.logo_url` carries that preview URL while public and search responses keep the versioned public URL. Administrator `PUT/DELETE /admin/projects/{id}/logo` enforce sessions, CSRF, and expected Project revision (409 on conflict). Search documents gained `logo_revision` and every Project response carries a nullable `logo_url`. The unified `ausectl project-content import-manifest` replaces the removed `ausectl artifacts import-manifest` and its CSV loader (tracked-reference sweep confirms only historical records and the parser test asserting rejection remain; `seed-demo` and `migrate` kept); it loads one strict versioned JSON manifest (unknown fields, wrong version, duplicate identities, blank strings, and trailing data rejected; the document must end at EOF, and a Project resolved twice through different identity forms is rejected after resolution before any write), plans the whole bundle before any write, and applies per Project through the shared `projectpool` bounded dispatcher (logo first, then sequential files, digest-unchanged skips, per-item domain validation). Frontend identity components render the logo in a fixed footprint with an initials fallback on missing or failed images (decorative alt, error fallback), search results and Project headers use them, and the admin Project form gained upload/replace/remove controls with revision-conflict and invalid-file feedback. Verified 2026-09-12: whole-backend `go vet` and the full PostgreSQL-backed test suite pass including a full HTTP boundary matrix (published+matching 200 with headers, all 404 cases, missing-version 400, storage 503, unauthenticated 401, missing/invalid CSRF 400/403, invalid PNG 400, stale revision 409, successful upload/replacement/removal with strict version increase), a logo projection test (document revision follows the active row, nil when absent or removed), a `searchResponse` logo URL test, loader and lifecycle integration tests, and race detection for the four concurrency-bearing packages; frontend lint, `tsc -b`, 70 component tests, and the default-subpath production build pass; generation is deterministic and compose renders. Extractor evidence: the jq export from `tools/ausesp-data-extractor` commit `153408e` produced exactly 71 logo entries, all valid bounded PNGs (max 1,430,125 bytes, 1600 px), and the guarded bundle test planned all 71 through the real importer (14,757,652 bytes). Operator note: `.memory/sasta-dev-notes/project-content-import.md`. The HTTP logo upload handler bug (multipart body length passed as the file's expected size, caught by the boundary test) was fixed to use the file part size. Correction round from independent review, applied the same day in a follow-up commit: logo revisions are now derived from all historical rows so a remove-and-re-upload can never reuse `?v=1` (the soft-deleted row also consumes one revision, so a replacement serves the prior version plus two and both earlier versions 404), stale-version lookups are resolved entirely from metadata and a counting-storage test proves no provider open occurs, PNG validation fully decodes the buffered image after the bounded configuration decode (new `ErrCorruptContent` sentinel; truncated IDAT data, corrupt chunk checksums, and missing terminal chunks are rejected with dedicated corruption tests), duplicate resolved Projects are rejected after resolution before any write, the manifest loader requires a true EOF (second document, stray brace or bracket, and trailing non-JSON text rejected; whitespace-only trailing content stays valid), the public detail initials fallback gained the purple or red identity background, the detail header places the logo beside the title on wide screens and stacks below 720 pixels, image-failure state resets when `logo_url` changes in the public identities and the administrator preview, and persisted draft logos preview through the authenticated administrator endpoint while anonymous draft access stays 404. Not done by design: no Playwright or live acceptance, no live B2 bulk import, no stack rebuild or deployment; those wait for the VM milestone with items 4 and 5.

14. **Implemented and accepted.** Storage provider readiness implemented 2026-09-12 per `.memory/docs/increments/13-storage-provider-readiness.md`. Previous gap: readiness checked only the PostgreSQL schema and the metadata-import temporary directory, so during a B2 outage or after credential rejection `/health/ready` and `/api/v1/health/ready` kept returning 200 while Artifact and Project Logo reads and writes failed. Implemented: the `Backend` interface gained `CheckReady(ctx)`; `LocalStorage.CheckReady` applies the write path's symlink validation to the root (a symlinked root fails instead of probing through the link), verifies the root exists as a directory, and creates, closes, and removes one empty `.ready-probe-*` file directly inside it (a pre-canceled context returns unavailable without touching the filesystem, the write path's on-demand temporary directory is not required, and a root that disappeared after startup fails without being recreated); `B2Storage.CheckReady` issues exactly one read-only S3 `ListObjectsV2` with `MaxKeys=1` and prefix `v1/` through the shared provider instance, proving endpoint, signing, credentials, bucket access, and the bucket-scoped `listFiles` capability without needing `listAllBucketNames` (deliberately not `HeadBucket`, so a bucket-restricted application key works; response contents are irrelevant and an empty bucket is healthy); every SDK, HTTP, authentication, authorization, missing-bucket, network, deadline, or cancellation failure maps to `ErrStorageUnavailable` with safe internal context and never to a healthy or content-absent signal. The B2 probe is bounded by a narrow single-flight controller: at most one request in flight for the shared instance, concurrent callers join the leader through a closed-channel broadcast while honoring their own cancellation without disturbing the leader, successes cache for 30 seconds and failures for 5 seconds through an injectable clock, and the controller holds no credentials, object names, or response bodies. `StorageSet.CheckReady` probes only the configured default provider so a secondary provider registered for migration or historical reads cannot make the application unready. The configuration-level probe now covers only the metadata-import temporary directory (`CheckImportTemporaryDirectory`; startup `EnsureStorageDirectories` still creates and verifies both local directories), and `httpserver.CheckReadiness` composes schema, import directory, and default provider, receiving the same shared `StorageSet` instance from `cmd/api/main.go` and `NewAPIHandler` so coalescing and caching cover both the unversioned and versioned readiness endpoints. Liveness never touches PostgreSQL or storage, and responses disclose no provider details. The non-persistent probe cannot prove `writeFiles` or `deleteFiles` capabilities; those keep failing safely through the existing storage-unavailable surface. A B2 storage without its constructed controller (nil receiver or missing readiness wiring) reports a controlled `ErrStorageUnavailable` instead of panicking. Verification 2026-09-12 (after the correction round): unit tests for local probe cleanup, unusable roots (missing, file, unwritable, and symlinked roots rejected through the write path's own symlink validation with the real target directory left untouched), and pre-canceled no-work; S3-stub tests proving exactly one `ListObjectsV2` with the bounded query (an empty bucket healthy), that the request is SigV4-signed with the configured key ID, that a malformed 200 XML response maps to `ErrStorageUnavailable`, and that 401/403/404/500/network/deadline/cancellation failures all map to `ErrStorageUnavailable` while keeping their distinct statuses in the safe internal context; deterministic clock tests expiring the 30-second success and 5-second failure windows (failures use a terminal 403 because the SDK retries 5xx, which would inflate request counts); coalescing proven without any sleeps through a stub arrival signal and a controller waiter hook: one request across a leader plus four waiters that all join before release, and a canceled waiter that provably waits before cancellation, returns unavailable, and neither disturbs the leader nor issues another request; `StorageSet` default-only behavior; config tests for the split import-directory probe; a PostgreSQL-backed composition test proving the provider participates in readiness, both endpoint forms return 503 with no private provider text while both liveness forms stay 200; and a real-wiring test proving the unversioned `HealthHandler` route and the versioned `NewAPIHandler` route over one shared `B2Storage` instance issue exactly one signed remote request inside the success cache window. Race detection passed for the artifacts package. `go vet` whole-backend and tracked gofmt clean. The opt-in live B2 check RAN against the real bucket through `.env.local-test`: the existing bucket-restricted key passes the probe (listFiles is already granted, so no key change is required), the first request completed in about 0.95 seconds, and the cached second check issued no second request; no object was created or deleted and no secret was printed.


15. **Implemented.** Internal operator notes were consolidated after acceptance of storage-provider readiness. `.memory/sasta-dev-notes/project-deploying-commands-and-explanations.md` is the active reference for fresh local Compose deployment, manual Docker Hub `linux/amd64` publication, VM rollout, readiness, reset, and future backup boundaries. `.memory/sasta-dev-notes/project-content-import.md` now covers both mock Project File seeding and the unified Project Logo plus Project File manifest workflow. The old runbook and duplicate active guides were removed; the original Artifact rehearsal and earlier deployment notes moved under the correctly named `archives/` directory. The reviewed metadata CSV and Project Content bundle remain the current development source of truth, and end-to-end import verification remains a manual maintainer step.

16. **Open, report first and change only after maintainer review.** Conduct a codebase simplification audit after the current publication cycle. Identify duplicated logic, obsolete development compatibility paths, unjustified abstractions, unreachable defensive branches, stale naming, and complexity introduced by agentic implementation. Explain why each candidate exists, its current value, and the risk of removing it. Preserve real replacement boundaries such as storage providers, search projection, versioned catalogs, and malleable taxonomy. Implementation follows only for candidates explicitly accepted after the report.

17. **Open, later product increment.** Redesign the administrator experience for sustained real-world maintenance rather than the current development import workflow. The future pass must cover Project and Person discovery, role assignment, large option sets, validation and conflict recovery, Project Files and Logos, bulk operations, and efficient editing without assuming that administrators will recreate the source CSV workflow. Scope and interaction design require discussion before an implementation brief.

18. **Implemented as Increment 15, 2026-09-12** (`.memory/docs/increments/15-project-repository-links.md`). Migration 00004 adds `project_repository_links` (schema version 4): ordered Project-owned metadata with URL length and nonempty checks, availability constrained to accessible/not_accessible/unverified, nonnegative sort order, unique URL and unique sort order per Project, and at most one primary per Project through a partial unique index. `projectlinks.Service` validates strict HTTPS URLs (absolute scheme, nonempty host, no embedded credentials, no control characters, no fragments, at most 2048 characters, duplicate rejection after normalization), requires exactly one primary link for a nonempty set and a `checked_at` timestamp, and replaces a declared set transactionally: Project row lock with expected-revision enforcement, deleted-Project rejection, unchanged sets write nothing, changed sets delete and reinsert in one transaction, one revision increment, one `project_repository_links.replaced` audit event with old count, new count, and resulting revision, and pending search synchronization through the normal upsert action. The Project Content manifest version 1 gained an optional authoritative `links` array: omitted leaves the Project's links unchanged, present arrays (including empty) are the complete desired set, and JSON null is rejected through a presence-preserving decoder; every link must explicitly carry `url`, `primary`, `availability`, and `checked_at` (an omitted `primary` is rejected rather than read as false, explicit `primary: false` stays valid, and unknown fields remain rejected), and loader-level URL and RFC 3339 timestamp checks plus planner-level full set validation stop whole-manifest planning before writes. URL validation requires a nonempty `Hostname()` (so `https://:443/repo` is rejected) and bounds the canonical normalized form to 2048 characters in runes, matching PostgreSQL `length(url)` on the stored value. Apply order within one Project is logo, files, then links with a fresh revision read, and every declared link set is rechecked under the transactional Project lock through the authoritative Replace: the planning classification serves dry-run reporting only, so a set changed after planning is still restored and reported as replaced. Progress and summaries use repository-link language (`links replaced (N repositories)`, `links unchanged (N repositories)`, `links failed` with the controlled error), and links never contribute to upload or byte totals. Migration 00004 is proven reversible: the table exists at schema 4, disappears after a rollback to schema 3, and returns after migrating up again. Public Project responses carry a required always-present `repository_links` array (never on summaries, search results, or person pages), and the public Project page renders a Repositories section only when links exist: visible URL text with overflow wrapping, `target="_blank"` with `noopener noreferrer`, a text primary label when multiple links exist, text availability labels (Accessible in the success color as a supplement, Not accessible (private or deleted), Unverified), and the localized exact check date; every link stays clickable and no runtime liveness request exists. Verification: PostgreSQL-backed service tests for validation, ordering, revision and audit counting, unchanged rerun with no writes, changed-set replacement, stale revision, deleted Project, and explicit empty-set removal; schema constraint tests rejecting duplicate URLs, duplicate order, multiple primary rows, invalid availability, negative order, blank and overlong URLs; manifest loader tests for null rejection, presence versus emptiness, unknown link fields, and missing or malformed fields; importer integration tests covering link-only and combined entries, dry-run classification (a declared empty set over an empty stored set classifies unchanged), apply, progress items, exact rerun skipping, invalid data stopping all writes, and omitted links leaving sets untouched; an HTTP test proving ordered public projection with the always-present empty array; and the guarded extractor evidence run planning exactly 13 Projects and 15 links (13 accessible, 1 not accessible, 1 unverified) with all 47 third-party references and the 2 sp-1651 links excluded at bundle generation. Whole-backend vet and tests, frontend lint, type checking, 79 component tests, and the default-subpath production build pass.

## Post-MVP correction backlog (2026-09-05, live import testing)

- **Partially implemented:** the select-all-valid-rows action is implemented (2026-09-08) as one persisted row update that walks every preview page and selects only rows whose server state is valid. An acknowledge-all-warnings action remains open; error rows still require correction and ambiguous duplicates still require explicit decisions.
- **Open:** import preview rows wrap text permanently; browser zoom does not restore layout. Long values need bounded truncation or normal wrapping inside a horizontally scrollable table region.
- **Implemented:** the SP metadata extractor moved to its own repository, `tools/ausesp-data-extractor` (remote `git@github.com:sasta-kro/ausesp-data-extractor.git`, nested and gitignored by the main repository). The main repository consumes generated deliverables rather than the extraction pipeline.
- **Implemented:** the extractor reached agent-reviewed corpus quality. `ause-discovery-projects-metadata-import.csv`, built from ground-truth records, is the metadata source of truth; the superseded regex pipeline output is not used.

## Corpus extraction status (2026-09-09, full corpus)

The entire corpus is now agent-read: 217 report-bearing projects (145 in the discovery pass plus the final 72 in a 24-agent round, 3 documents each, whole-document reads with txt fallbacks and in-agent verification). `output/reviewed-import.csv` has 212 rows (145 CS / 67 IT), only 5 drops remain (1651, 1661, 1813, 1904, 2146 print no semester or year anywhere). The taxonomy suggestion report covers all 217 projects (`tools/ausesp-data-extractor/output/discovery-report.md`). Corpus duplicates confirmed as same project under two numbers (kept, flagged for import deselection): 1703/1711, 1934/2003, 1906/2004, 1827/1920, 2579/26009, 2113/2145. sp-1628 and sp-1800 remain advisorless (verified); sp-1628 has no platform. Shared student ids across rows are all same-person spelling variants (importer merges them by id, which is desired). Agent PDF reading on this machine requires poppler (`brew install poppler`); without it the Read tool fails on many 2022+ PDFs and the pypdf txt fallbacks in `.tmp-prepared/` carry the load. Import note: aliases equal to their row title (case-insensitive) are filtered at build time since the importer rejects them. Extractor commits through 6185ac1.

## SP1/SP2 classification pass complete (2026-09-12)

Third corpus pass, extractor commits `2503d58` + `ba0bee4` (student cross-check), full decision record in the extractor `notes/sp1-sp2-pass.md`. Every project now carries a course classification: the CSV has 209 rows split 103 `senior_project_1` / 100 `senior_project_2` / 6 unspecified (1636, 1653, 1657, 1801, 1933, 25101 — no explicit statement, codes absent or conflicted, and no usable student cross-evidence; 1933's cross-evidence is self-contradictory). Method per maintainer: explicit document statement first, then unanimous dictionary inference from printed course codes, then unspecified. The `phase` field does NOT indicate course (SP1 and SP2 are unrelated projects). Course-code dictionary with sighting counts and exceptions lives in the note; per-document explicit statements always override it (several 2025 documents print SP2 under SP1-numbered codes: 1823, 2222, 26022, 26030, 25117).

Duplicate pairs fully culled (worse side deleted everywhere: ground truth, enrichment, logos, CSV): kept 1703, 1934, 2004, 1920, 2579, 2113; deleted 1711, 2003, 1906, 1827, 26009, 2145. The old "deselect one side at import" instruction is obsolete.

Slide-only projects transcribed from full decks into `batch-slideonly.json` and now importable: 2021 Randevoo (sp2 explicit, Sokvathara Lin + Menh Keo ids attached from their other projects), 2031 Hotel Management (3 printed ids), 2032 Dental Inventory (Tanakorn Navanugraha + Rajbir Singh ids attached from 2017; Kan Wuthithepbunha id-less, documented exception #5). Their semester/year values are the only inferred academic periods in the dataset (from Gantt charts/deck content).

Verification outcomes: 2238's student list is complete (three students, no missing person); 1934's title was already clean. Logo manifest now generated CSV-membership-filtered (`steps/apply_sp_pass.py`); the main-repo operator note's hardcoded 2021 exclusion was replaced by the membership filter in commit `c8bf834`.

## Enrichment pass complete: project logos and repository links (2026-09-11)

Executed 2026-09-11 over all 220 corpus projects (217 report-bearing + 3 slide-only: 2021, 2031, 2032). Poppler 26.09.0 installed beforehand. Products committed to the extractor repo (commit `59fced5`, restructured into `output/enrichment/` by `153408e`: records, logos, manifest, link-kinds, liveness, summary, text-grep) plus eight pipeline scripts under `steps/` (prepare, render, grep, crop, assignments, liveness, classify, merge).

Results:

- Logos: 71 of 220 projects have an extracted project-specific logo PNG (`enrichment/logos/<id>.png`, cropped at 300 DPI from report covers or slide title pages, auto-trimmed, max 1600 px). 112 are `au_crest_only` (pre-2021 reports almost never carry a project mark), 34 `no_logo_found`, 2 `no_viewable_source` (1636, 1906 legacy .doc with no recoverable images), 1 found-but-not-extracted (2008, mark exists only inside app screenshots). Initials remain the fallback rendering.
- Repository links: 61 distinct URLs found, ALL through the mechanical text grep (visual agents found zero on-screen repo URLs in slides; slides carry demo links like heroku/railway/github.io instead, which are out of scope). Hand classification: 17 `project_repo` (owner matches a ground-truth student name, or repo name matches the project title) across 14 projects, 43 `third_party_reference` (cited libraries such as tesseract, zxing, alamofire, leveldb, whisper, and similar are kept for provenance, not for display), 1 dropped scrape artifact. Only `project_repo` links are meant for public display.
- Liveness (checked 2026-09-11): of the 64 kept links, 52 public, 10 not_found (private or deleted), 2 unknown. `not_found` displays as "Not accessible (private or deleted)"; `unknown` as "Unverified"; statuses carry `checked_at` and the post-redirect `final_url`.
- Display spec (unchanged, for the future app increment): every project_repo link shown regardless of status, green accessible indicator for public, status age displayed.

Audit: mechanical validation of all 220 records and PNGs passed; a flash-agent spot-check of 18 logos confirmed 14, downgraded 2148 (pill button, not a logo), re-cropped 1824 from its poster (whole poster was initially copied), kept 2119 (stylized wordmark). 2039's award certificates contain no repo URLs.

Corpus facts learned: 1651's report ZIP contains both students' repos (`chawanvtp/sakpha-android` public, `aretisd/sakpha-` gone); duplicate pair 1703/1711 share the same embedded logo image; demo-site URLs (github.io, herokuapp, railway) exist for several projects and could be a future "deployed site" link type.

App integration decision recorded 2026-09-11 in `.memory/docs/increments/12-project-logo-integration.md`: a Project Logo is optional Project presentation metadata with its own records and service. Logo bytes reuse the configured object store, but logos are not Artifacts and must not appear in Project File lists, Artifact counts, facets, or download controls. One versioned Project Content JSON manifest maps each existing Project to its optional logo and Project Files, then dispatches each item to its correct domain service. The superseded flat Project File CSV importer does not require compatibility. The metadata import CSV remains unchanged. Repository-link integration remains deferred. Liveness statuses go stale and should be re-runnable (`steps/check_link_liveness.py`) before any launch.

Orchestration constraints (used, for future passes): concurrency 12 (429 cliff observed at ~19-20), 3 waves of 12 agents x 6 projects, 8 stalls recovered via resume messages, zero rule violations, no CPU incidents. Visual reads were served by the haiku slot (glm5.3-flash class); the main session never reads images (glm5.3 image reads reroute to glm4.6v).

## Person-name canonicalization in the import CSV (2026-09-11)

Live-import bug reports (duplicate People rows in the admin UI) traced to spelling variants the importer cannot merge (it matches exact 7-digit student_id or case/space-insensitive display_name only). Full-corpus review: 486 raw spellings, 416 post-normalization names, 38 fuzzy pairs. Fix: hand-reviewed `CANONICAL_PEOPLE` map in `steps/build_reviewed_csv.py` (ground truth stays verbatim per repo policy). Distinct CSV names 411 -> 380 after rebuild. Extractor commits `6650830` (fix) and `153408e` (repo restructure); full analysis and policies in the extractor `notes/person-name-canonicalization.md` and `notes/people-duplication-review.md`.

Key policies, confirmed by the maintainer: Burmese names have no surnames (never merge on shared name elements, spacing insignificant, Phone Pyae Kyaw Swar manually spaced); distinct student ids are definitive (Vibolrottana/Vibolrottanak Seng are two students); the university has only student ids, no staff ids; four students (Kamonchanok Arttanate, Nathanan Pornprapee, Nattalie Shinkoi, Sai Kham Sheng) import without ids as documented exceptions, unrecoverable through the university, possibly recoverable from paper records later. Spelling tie-breaks used self-evidence: Paranan Vitpornnitipacha (her GitHub/LinkedIn), Taechasit Sarasitt (his GitHub username), Setthanant Tetanonsakul (Thai-romanization judgment). Fifteen professor families collapsed, including the six-spelling Anilkumar Kothalil Gopalakrishnan.

The app databases still hold the old misspelled people and stale participations from earlier imports (the student-Anilkumar row the maintainer screenshotted never existed in the current CSV). The clean path is a fresh import into a clean database; re-importing over old data merges by name but leaves orphan rows needing admin deletion.

## Corpus extraction status (2026-09-08)

142 of 217 report-bearing corpus projects are agent-reviewed (42 from the thinned sample + 100 new, ids 1616-2127, run in two 10-agent waves). The import CSV (`tools/ausesp-data-extractor/output/reviewed-import.csv`) now has 111 rows (78 CS / 33 IT, years 2017-2025); 31 records dropped for missing abstracts (proposals by design; 1661/1813/1904 also lack semester/year). Person names are stripped of honorifics and academic titles at build time (builder commit `54a19a5`). Known corpus quirks recorded in the extractor README: duplicate projects under two numbers (1703/1711, 1934/2003, 1827/1920, 1906-final/2004-proposal), three RAR archives unreadable without `unar` (1651, 1902, 1940), 14 Word-doc reports converted to text, `php` not in the taxonomy (stripped; candidate values.yaml addition), sp-1800 prints no advisor. Remaining ~75 projects await a future wave; wave prompts and chunking live in this session's transcript, batch files in `ground-truth/batches/batch-100-*.json`.

## Legacy source-file numbers (2026-09-05)

The numeric prefixes on senior-project source files (1825, 2006, and similar) are opaque legacy identifiers inherited from a previous university system. Their semantics are unknown. Decision: keep the numbers internal only, as the file-name source of `import_key` (`sp-<number>`) and loaded into `projects.reference_code`; never display them publicly. History: the reviewed CSV briefly emitted an empty `reference_code` as a temporary fix (2026-09-05). Superseded on 2026-09-08 by the permanent split: the CSV emits the number into `reference_code` (so the DB keeps it for admin search and any future institutional confirmation), and the public UI stopped rendering `reference_code` everywhere (search results metadata, Project page eyebrow) in main-repo commit `48db014`. The admin Project form and import review still show it. If a public label is ever wanted, it must wait for institutional confirmation of what the numbers mean.
